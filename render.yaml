services:
  - type: web
    name: qa-confirm-gate-render
    env: python
    rootDir: PRODUCT/Render GDrive/source
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
