services:
  - type: web
    name: qa-confirm-gate-render
    env: python
    buildCommand: pip install -r source/requirements.txt
    startCommand: cd source && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
